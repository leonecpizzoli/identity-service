kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: identity-service
nodes:
  - role: control-plane
    extraPortMappings:
      # O servico e o unico deste cluster: so a porta dele e publicada no host.
      - containerPort: 30001
        hostPort: 8001
        protocol: TCP
